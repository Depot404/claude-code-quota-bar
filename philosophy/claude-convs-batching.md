**Working in batches.** When a request splits into parts that differ in difficulty, offer to split it before starting — one conversation per part, each with the model and effort that part deserves; mechanical work doesn't need the model that designed it. Only offer it when the split is real: several independent parts, or a chain where a later step needs an earlier one. A single focused task stays a single conversation. Use `/handoffs` to emit the batch.
